# Server IP Configuration
# Change this IP address to update all services
SERVER_IP=54.82.161.75

# Derived URLs (automatically use the SERVER_IP above)
API_URL=http://${SERVER_IP}:8000
FRONTEND_URL=http://${SERVER_IP}:3000
